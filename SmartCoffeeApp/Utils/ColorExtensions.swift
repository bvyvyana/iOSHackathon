import SwiftUI

extension Color {
    // App colors - using unique names to avoid conflicts
    static let primaryRed = Color("AppRed")
    static let primaryGreen = Color("AppGreen") 
    static let primaryBlue = Color("AppBlue")
    static let primaryOrange = Color("AppOrange")
    
    // Coffee colors
    static let coffeeBrown = Color(red: 0.6, green: 0.4, blue: 0.2)
    static let latteCream = Color(red: 0.95, green: 0.9, blue: 0.8)
    static let espressoDark = Color(red: 0.2, green: 0.1, blue: 0.05)
}